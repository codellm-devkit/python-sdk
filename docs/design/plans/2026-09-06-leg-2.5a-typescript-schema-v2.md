# Leg 2.5a — TypeScript on schema v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the TypeScript layer of the SDK read codeanalyzer-typescript 1.2.0 artifacts — JSON and Neo4j — with every existing accessor keeping its name, signature and return type, and the language-neutral machinery leg 1.6 built for Python lifted to `commons/` first so nothing is ported by copy.

**Architecture:** Four moves in dependency order. (T0) Lift the generic bounds/paging/validation/key helpers from `cldk/analysis/python/` into `cldk/analysis/commons/`, Python re-importing them — a pure move pinned by Python's existing suite. (T1) Replace the v1 TypeScript models with a pydantic mirror of cants' `schema.ts` at 1.2.0, validated against real `-a 1..4` output. (T2) `TSAnalysisBackend` inherits the generic ABC; `TSCodeanalyzer` invokes 1.2.0 correctly and loads the v2 envelope; pin 1.2.0. (T3) `TSNeo4jBackend` re-pointed statement by statement at the 1.2.0 vocabulary with two-prefix scoping and a generation-aware probe. (T4) Live verification on the superset graph, a multi-application audit in the leg-1.6 shape, docs.

**Tech Stack:** Python 3.11+, pydantic v2, neo4j driver 5.x, codeanalyzer-typescript 1.2.0 (bun-compiled binary in the PyPI wheel; `--emit neo4j` always full depth; `-j` falls back to sequential in the wheel — cants #171).

**Spec:** `docs/design/specs/2026-09-06-leg-2.5-typescript.md` (G1–G5, TS-1–TS-4). Tracking: python-sdk#335 under codellm-devkit/.github#55.

## Global Constraints

- **The public API does not move (G3, Iron Rule).** No accessor on `TypeScriptAnalysis` or `PythonAnalysis` changes name, signature or return type. The only removals are the two TS accessors that only ever raised `NotImplementedError` (`get_entry_point_methods`, `get_service_entry_point_methods`), replaced in 2.5b.
- **Per-language models (G1, leg-1 D2).** The rung's wiring reference prescribes shared `cldk/models/cpg/` + views; the committed spec chose per-language models. The spec governs. Do not revive `cldk/models/cpg/`.
- **This item does not touch the query surface** (addressing, scoping keywords, per-callable pages, slices, paths, entrypoints, artifacts) — that is 2.5b (#335's sibling, filed when this lands).
- **Read-only against every Neo4j instance.** TypeScript reference graph: `bolt://localhost:7690`, user `neo4j`, password `cldkleg25test`, application `superset-frontend` (emitted from cants `main@977198e`: no `_module`, body-node ids present, stamps `analyzer_version 1.2.0`). Python regression graph: `bolt://localhost:7689`, password `cldkleg16test`, application `odoo-slim-19`. **Port 7687 is an ssh tunnel; never a target.**
- **`tests/analysis/typescript/test_typescript_neo4j_backend.py` is live-gated on `CLDK_TEST_NEO4J_*` — read it before running it against any graph; if it emits, treat it like the Python writer (#324) and gate it on a distinct `_WRITE_` variable.** `tests/analysis/python/test_python_neo4j_backend.py` is never run.
- Run suites **sequentially** (parallel runs share the coverage file). Stage by name; never `git add -A`. Work in the worktree `<scratchpad>/wt-leg25a` (branch `feat/issue-335-ts-schema-v2`); the main checkout stays on `release/2.0`.
- Floors at `a9359c8`: exact release gate `uv run pytest --pspec --cov=cldk --cov-fail-under=33` = **686 passed / 187 skipped**, coverage 76%; Python live on 7689 = **590 passed**; TypeScript offline = **101 tests** across 9 modules; contract files green.
- No `can://` or ordinal in any signature, return field other than `ref`/`node_id`/`next_cursor`, or error message (E6/E7). No suggestions or fuzzy matching anywhere (E8).
- **Never add Claude/AI attribution** to any commit, comment, doc or changelog entry.

---

## Task 0: Lift the language-neutral helpers to `commons/`

A pure move. Nothing Python-shaped leaves `cldk/analysis/python/`; nothing that moves gains or loses behaviour. Python's suites are the proof.

**Files:**
- Create: `cldk/analysis/commons/bounds.py`, `cldk/analysis/commons/graphs.py`, `cldk/analysis/commons/keys.py`
- Modify: `cldk/analysis/python/backend.py`, `cldk/analysis/python/neo4j/reconstruct.py`, `cldk/analysis/commons/resolve.py`, `cldk/analysis/python/neo4j/neo4j_backend.py`, `cldk/analysis/python/codeanalyzer/codeanalyzer.py` (imports only)
- Test: the existing Python tests that import these names keep importing them from `cldk.analysis.python.backend` (re-exports) **and** a new `tests/analysis/commons/test_lifted_helpers.py` asserting each name is importable from its `commons` home and that `cldk.analysis.python.backend` re-exports it (`is` identity).

**Interfaces:**
- Consumes: nothing.
- Produces (exact names, unchanged signatures unless noted):
  - `commons/bounds.py`: `DEFAULT_PAGE_SIZE`, `DEFAULT_DEPTH`, `DEFAULT_MAX_NODES`, `DEFAULT_MAX_PATHS`, `check_depth`, `check_max_nodes`, `check_max_paths`, `check_page_size`, `check_distinct_endpoints`, `reject_bare_string`, `check_selector`, `encode_cursor`, `decode_cursor`, `keyset_where`, `cursor_params`, `edge_page`, `EdgeOrder`.
  - `commons/graphs.py`: `bounded_subgraph`, `hop_sort_key`, `slice_resolved`, `cone_sinks`, `as_slice_node`, `flow_path(hops, *, via: Mapping[str, str])` — **`VIA` becomes a parameter**; `edge_sort_key(kind: Literal["cfg","cdg","ddg"])` returning the structural key functions (`cfg_sort_key`/`cdg_sort_key`/`ddg_sort_key` stay in `python/backend.py` as the `py_schema`-typed specialisations that call it); `sdg_rels(P: str) -> tuple[str, ...]`, `sdg_rel_pattern(P: str) -> str`, `via_table(P: str) -> dict[str, str]` — **the `PY_*` tables become functions of the ABC's `P` prefix**, with `SDG_RELS`/`SDG_REL_PATTERN`/`VIA` in `python/backend.py` defined as `sdg_rels("PY")` etc.
  - `commons/keys.py`: `resolve_module_key`, `scope_paths`, `call_graph_scope`, `module_key_of(node_id, prefix, known)` (moved from `python/neo4j/reconstruct.py:72`, behaviour identical), `module_dotted(path, *, extensions: Sequence[str] = (".py",))` — **the `.py` becomes a parameter with the Python default**, so Python's call sites are unchanged.

- [x] **Step 1: Write the import-surface test first**

```python
# tests/analysis/commons/test_lifted_helpers.py
import importlib, pytest

LIFTED = {
    "cldk.analysis.commons.bounds": ["DEFAULT_PAGE_SIZE", "DEFAULT_DEPTH", "DEFAULT_MAX_NODES", "DEFAULT_MAX_PATHS",
        "check_depth", "check_max_nodes", "check_max_paths", "check_page_size", "check_distinct_endpoints",
        "reject_bare_string", "check_selector", "encode_cursor", "decode_cursor", "keyset_where", "cursor_params",
        "edge_page", "EdgeOrder"],
    "cldk.analysis.commons.graphs": ["bounded_subgraph", "hop_sort_key", "slice_resolved", "cone_sinks",
        "as_slice_node", "flow_path", "edge_sort_key", "sdg_rels", "sdg_rel_pattern", "via_table"],
    "cldk.analysis.commons.keys": ["resolve_module_key", "scope_paths", "call_graph_scope", "module_key_of", "module_dotted"],
}

@pytest.mark.parametrize("module,names", LIFTED.items())
def test_each_helper_lives_in_commons_and_python_reexports_it(module, names):
    home = importlib.import_module(module)
    py = importlib.import_module("cldk.analysis.python.backend")
    for n in names:
        assert hasattr(home, n), f"{n} missing from {module}"
        if hasattr(py, n):
            assert getattr(py, n) is getattr(home, n), f"{n} re-exported from python.backend is a copy, not the same object"

def test_python_tables_are_the_P_prefixed_instances():
    from cldk.analysis.commons.graphs import sdg_rels, via_table
    from cldk.analysis.python import backend
    assert backend.SDG_RELS == sdg_rels("PY")
    assert backend.VIA == via_table("PY")
    assert sdg_rels("TS")[0].startswith("TS_")

def test_module_dotted_defaults_to_python_and_takes_other_extensions():
    from cldk.analysis.commons.keys import module_dotted
    assert module_dotted("odoo/tools/mail.py") == "odoo.tools.mail"
    assert module_dotted("src/pages/Home.tsx", extensions=(".ts", ".tsx")) == "src.pages.Home"
```

- [x] **Step 2: Run it, watch it fail on `ModuleNotFoundError: cldk.analysis.commons.bounds`**

- [x] **Step 3: Move, don't rewrite.** Cut each function/class from `python/backend.py` (and `module_key_of` from `reconstruct.py`, `module_dotted` from `resolve.py`) into its `commons` home byte-for-byte except the three parameterisations above. In `python/backend.py`, import them back and keep the old names bound (`from cldk.analysis.commons.bounds import check_depth, …` plus `SDG_RELS = sdg_rels("PY")` etc.). `reconstruct.py` and `neo4j_backend.py` import `module_key_of` from `commons.keys`. Where a moved function referenced a `py_schema` type only in an annotation, keep the annotation as a string or `Protocol` so `commons/` imports nothing from `codeanalyzer.*`.

- [x] **Step 4: Prove `commons/` is language-neutral** — *as done:* the runtime one-liner cannot print `[]` at HEAD (`cldk/__init__` loads the whole SDK; `commons/results.py:55` imports `cldk.models.python`), so the proof is static: the AST of each new module lists no import from `codeanalyzer.*` or `cldk.models.*`. Verified for `bounds`, `graphs`, `keys`.

- [x] **Step 5: Python is unchanged.** *As done:* gate 691 / 181 (686 + 5 new; the 6 fewer skips are the writer file's env-gated tests), live 590, public surface 19. One source-text pin (`test_dataflow.py::test_the_rule_is_stated_once_and_cited_by_all_five`) followed `DEFAULT_DEPTH` to `commons/bounds.py`; `flow_path(..., via=VIA)` at its three call sites. Sequentially: the exact release gate (floor **686 / 187**, coverage ≥ 76%); `tests/analysis/python/ tests/models/python/` live on 7689 (floor **590**); `tests/test_public_surface.py` byte-identical result. Any count change is a defect in the move.

- [x] **Step 6: Commit**

```bash
git add cldk/analysis/commons/bounds.py cldk/analysis/commons/graphs.py cldk/analysis/commons/keys.py cldk/analysis/python/backend.py cldk/analysis/python/neo4j/reconstruct.py cldk/analysis/python/neo4j/neo4j_backend.py cldk/analysis/commons/resolve.py tests/analysis/commons/test_lifted_helpers.py
git commit -m "refactor(commons): lift the language-neutral bounds, paging and key helpers out of the Python backend

A pure move. The validation, keyset paging, edge ordering, bounded-subgraph,
path-assembly and module-key helpers leg 1.5/1.6 built contained nothing
Python-specific except three things, now parameters: the PY_ relationship
prefix, the .py extension, and the VIA map. Python re-imports every name, so
its suite is the proof that nothing changed. TypeScript (leg 2.5) and Java
(leg 2) inherit the same rulings instead of forking them."
```

---

## Task 1: TypeScript v2 models

> Refined from the schema inventory (`schema.ts` at `v1.2.0`, `emit.ts`'s `stripInternal`, `ids.ts`, `attach.ts`). Decisions TS-5 … TS-10 below are recorded in `.claude/FACADE_DECISIONS.md`.

**What the wire actually carries (authority: `v1.2.0:src/schema/schema.ts`, stripped by `emit.ts:47-70`):**

- Envelope `TSAnalysis{schema_version:"2.0.0", language:"typescript", max_level, k_limit?, analyzer{name,version}, application}`. `k_limit` only at L3+.
- `TSApplication{id, kind:"application", symbol_table, call_graph, param_in, param_out, artifacts, dependencies, unresolved_imports, config_uses, config_reads, external_symbols?, synthesized_callables?}` — `config_reads` (not Python's `config_reads_unresolved`); `external_symbols` is keyed by the entry's own `id` (`TSExternalNode`); **`synthesized_callables` is a compatibility index: its keys are the *older* anonymous-callable ids (`…/Controller@5:10`) and each value's `id` is the tree id that replaced it (`…/Controller/<anon@5:10>`) — key ≠ `id` in every fixture** (`TSSynthesizedNode`; residual fallback nodes are the only key == id case). An index over it must say which id it keys. `main` adds `entrypoint_report` (required there).
- `TSModule{id, kind:"module", span, source, imports, exports, comments, types, functions, fields, is_tsx, is_declaration_file, content_hash?}` — **no `file_path`/`module_name`** (the map key is the file key); `last_modified`/`file_size` are stripped.
- `TSType{id, kind ∈ class|interface|enum|type_alias|namespace, span, name, signature, comments, is_exported, is_ambient, + optional facets}`: `callables` (class/interface), `fields` (class/interface/**enum members**/namespace), `types`+`functions` (namespace), `decorators`, `base_classes` (extends ∪ implements, signatures), `implements_types`, `is_abstract`, `type_parameters`, `call_signatures`, `index_signatures`, `is_const`, `aliased_type`, `extends_ids`, `implements_ids`.
- `TSCallable{id, kind ∈ function|method|constructor|getter|setter|arrow|function_expression, span, name, signature, comments, decorators, parameters, type_parameters, overload_signatures, return_type?, accessibility?, accessor_kind?, cyclomatic_complexity, is_static, is_abstract, is_async, is_generator, is_optional, is_readonly, is_exported, is_ambient, is_implicit, body, callables?, types?, cfg?, cdg?, ddg?, summary?}` — **declared but never emitted: `abs_path`, `call_sites`, `config_accesses`** — do not model them; **no `code`, no `path`, no `accessed_symbols`, no `local_variables`, no `code_start_line`**.
- `TSCallableParameter{name, type?, default_value?, accessibility?, is_optional, is_rest, is_readonly, decorators, start_line, end_line, start_column, end_column}` (flat ints, not a span). `main` adds `id?`.
- `TSField{id, kind:"field", name, span?, type?, initializer?, scope?, declaration_kind?, is_exported?, comments?, decorators?, accessibility?, is_static?, is_readonly?, is_optional?, is_abstract?, value?}` — one open shape for module variables, class attributes, interface properties and enum members. `span` is absent for constructor parameter properties.
- `TSBodyNode{kind: str, span?, callee?: str|None, of?, parent?, method_name?, receiver_expr?, receiver_type?, argument_types?, type_arguments?, return_type?, is_constructor_call?, is_optional_chain?, root?, key?}`. Observed kinds at L4: `entry exit statement call config_access formal_in formal_out actual_in actual_out`. `callee` is the one sanctioned `null` (L1). `main` adds `id?`.
- Edges: `TSCfgEdge{src,dst,kind}` (kinds `fallthrough true false switch_case loop_back exception return break continue yield await_resume`), `TSCdgEdge{src,dst}`, `TSDdgEdge{src,dst,var?,prov: List[str]}` (1.2.0 emits only `["reaching-defs"]`; the analyzer's own comment reserves `ssa`/`points-to` — **not a `Literal`**), `TSSummaryEdge{src,dst,var?}`, `TSCallGraphEdge{src,dst,prov: List[str],weight}` (prov `tsc`/`defuse`/`import`, open), `TSParamEdge{src,dst,var?}` with **global** endpoints. Intra-callable edge endpoints are bare local keys.
- Artifacts: `TSArtifact{id, kind:"artifact", path, format, roles, size_bytes, sha256, source, extraction, config_keys}`, `TSDependency{name, spec, kind ∈ runtime|dev|optional|peer|build, extras, declared_in, direct, locked_version?, provides_imports, prov}` (no `ecosystem` — Python has it), `TSConfigKey{id, key, namespace, value?: str|int|float|bool, span?, references}`, `TSImportBinding{module, bound_to?, prov}`, `TSConfigUse{src,dst,prov}`, `TSConfigRead{site, callee, key?, reason, prov}`.
- Ids: application `can://typescript/<app>` (always `typescript`); module `can://<typescript|javascript>/<app>/<fileKey>` (`.js/.jsx/.mjs/.cjs` → `javascript`); type/callable = module id + `/` + the signature segments after the module prefix; field `<parentId>/<name>`; external `<appId>/@external/<module>/<name>`; synthesized `<enclosing>@<line>:<col>` or `<appId>/@synthetic/<encoded sig>`; artifact `can://artifact/<app>/<rel>`; config key `<artifactId>@key/<dotted>`; package `pkg:npm/<name>`.
- Body keys: `L:C`; L1 collisions `L:C/2`, `L:C/3`; **L3 collisions `L:C#<nodeId>`** (a different scheme); `@entry`, `@exit`, `@formal_in:N`, `@formal_out`, `L:C/actual_in:N`, `L:C/actual_out`. Global ordinal = `callableId + key` if the key starts with `@`, else `callableId + "@" + key` — identical to codeanalyzer-python's `global_ordinal`. Any key parser must not `split("/")` naively: `L:C/2` collides with the `L:C/actual_out` family.

**Decisions taken here (recorded as TS-5 … TS-10):**

- **TS-5 `types{}` is a discriminated union** `Annotated[Union[TSClass, TSInterface, TSEnum, TSTypeAlias, TSNamespace], Field(discriminator="kind")]`. G3 forces it: the five classes are the return types of existing accessors. `TSModule.classes/interfaces/enums/type_aliases/namespaces` become read-only `@property` filters over `types` so every 1.x attribute path keeps working. An unknown sixth kind fails validation loudly — the same drift alarm as `extra="forbid"`.
- **TS-6 `TSSpan` is modelled faithfully**; every model that used to expose `start_line`/`end_line` keeps them as computed properties over `span` (G3 for attribute access). Leaf models that the analyzer still emits with flat ints (`TSImport`, `TSComment`, `TSDecorator`, `TSCallableParameter`) stay flat.
- **TS-7 `code` is synthesised, not stored:** a `code` property slices `module.source[span.bytes[0]:span.bytes[1]]` as **characters** — `schema.ts:29-31` declares `bytes` as UTF-16 offsets. The Neo4j projection slices bytes (`project.ts:274-280`) and disagrees on non-ASCII files; filed upstream, and stated as documented lossiness between the two backends until it is fixed. The `TSCallableOverview` projection threads the owning module in so `code`/`start_line` keep their values.
- **TS-8 `extra="forbid"` stays, and 1.3.0's known additive fields are `Optional` now:** `TSBodyNode.id`, `TSCallableParameter.id`, `TSCallable.entrypoints`/`is_entrypoint`, `TSType.entrypoints`/`is_entrypoint`, `TSApplication.entrypoint_report` (required on `main`; `Optional` here parses both generations), `TSModule.call_sites` (internal on `main`, stripped — not modelled). The pin bump to 1.3.0 is then a deliberate step that changes no model, and any *other* drift still fails loudly.
- **TS-9 the call-edge class is `TSCallGraphEdge{src, dst, prov, weight}`**, with `TSCallEdge = TSCallGraphEdge` kept as an import alias. The field change is BREAKING-by-value in the CHANGELOG, as `PyCallEdge`'s was in leg 1. `TSExternalSymbol`/`TSSynthesizedCallable` are retyped to the wire's id-keyed `TSExternalNode{id, kind, module, name}` / `TSSynthesizedNode{id, kind, name?, path?, span?}`.
- **TS-10 the in-memory call graph stays signature-keyed** (as Python's `_id_to_signature` does), with the `can://` id as a node attribute — cross-language parity and G3. The index joins edges to nodes by id and maps to signatures once at load; `external_symbols` is indexed by id.
- **TS-11 TypeScript's call graph keeps its own endpoints.** cants emits modules as callers (top-level code) and classes as callees (`new X()`), e.g. `src/controllers.ts → controllers.ts/Controller`. Python's leg-1.6 rule (drop non-callable sources and class targets) would erase every top-level call, so `get_call_graph()` on TypeScript keeps them, with a `kind` node attribute (`module | class | callable | external`) so a caller filters to Python's shape in one line. The divergence is stated in the spec's §4 and the agent reference, not normalised away. Applies to both backends identically (T2 local, T3 Neo4j).

**Files:**
- Modify: `cldk/models/typescript/models.py` (rewrite), `cldk/models/typescript/projections.py` (field renames), `cldk/models/typescript/__init__.py`
- Test: `tests/models/typescript/` (new `test_ts_v2_models.py` validating a **real** 1.2.0 `analysis.json` at each level), the existing `test_ts_projections.py`

**Interfaces:**
- Produces: `TSAnalysis` (envelope: `schema_version`, `language`, `max_level`, `k_limit`, `analyzer`, `application`), `TSApplication`, `TSModule` (unified `types: Dict[str, TSType]` with a `kind` discriminant; the 1.x `classes/interfaces/enums/type_aliases/namespaces` accessors become kind-filters in the facade, not model fields), `TSType`, `TSCallable` (`id`, `span`, `body: Dict[str, TSBodyNode]`, `cfg`/`cdg`/`ddg`/`summary`), `TSParameter`, `TSField`, `TSBodyNode`, `TSDecorator`, `TSCallEdge{src, dst, prov, weight}`, `TSCfgEdge`/`TSCdgEdge`/`TSDdgEdge`/`TSSummaryEdge`, `TSParamEdge`, `TSExternalSymbol`, `TSArtifact`/`TSDependency`/`TSConfigKey`/`TSConfigRead`/`TSUnresolvedImport`, `TSEntrypointReport` (if present at 1.2.0), `TSSynthesizedCallable`.

- [x] **Step 1: Generate the fixture from the real analyzer.** *As done:* generated by the controller from the released wheel at `-j 1`, `--app-name slim`, committed with `git add -f` (`.gitignore:53` ignores `*.json`, as it did the 1.x fixture). `uvx --from 'codeanalyzer-typescript==1.2.0' cants -i tests/resources/typescript/application -a N -o <scratch>/ts-aN` for N in 1..4 (the wheel's `-j` fallback is fine at this size). Commit the four outputs under `tests/resources/typescript/analysis_json/v2/a{1,2,3,4}/analysis.json` — these replace the v1 `slim/analysis.json` as the blessed fixtures. Record the analyzer version they carry.
- [x] **Step 2: Failing test** — `TSAnalysis.model_validate_json(open(fixture).read())` for each level, plus assertions that pin the grammar: `application.id == "can://typescript/slim"` (the fixtures were generated with `--app-name slim`), a callable's `body` keys include `@entry`/`@exit` at level ≥ 3 and `@formal_in:0` at 4, a DDG edge's `prov == ["reaching-defs"]`, a call edge has `src`/`dst`/`prov`/`weight`.
- [x] **Step 3: Rewrite the models** as an `extra="forbid"` mirror of `schema.ts` at the tag — **forbid, so a 1.3.0 additive field fails loudly and the pin bump is a deliberate step**, unless the inventory shows 1.2.0 itself emits fields the tag's `schema.ts` does not declare; then say so and use `extra="ignore"` on exactly those models.
- [x] **Step 4: Projections** — `TSCallableOverview` and friends read the renamed fields; keep their public shape.
- [x] **Step 5: Run `tests/models/typescript/`** *As done:* 24 passed; byte-exact `model_dump(exclude_unset=True) == raw` at all four levels. `inner_classes` on a class is now always `{}` — the v2 `class` facet has no `types` (only namespaces and callables nest types), which is the analyzer's model, not a loss. Analysis tier after T1: 8 failed / 26 passed / 23 skipped / 46 errors, all at T2/T3 sites (listed in T2). — expected all green; the old `slim/analysis.json` is deleted with a note in the commit.
- [x] **Step 6: Commit** — `feat(typescript): v2 models mirroring codeanalyzer-typescript 1.2.0`.

---

## Task 2: `TSAnalysisBackend` on the generic ABC; `TSCodeanalyzer` on 1.2.0; the pin

**Files:**
- Modify: `cldk/analysis/typescript/backend.py`, `cldk/analysis/typescript/codeanalyzer/codeanalyzer.py`, `cldk/analysis/typescript/typescript_analysis.py` (bodies only), `cldk/analysis/commons/backend_config.py` (`TSCodeAnalyzerConfig.tsc_only` removed or made a no-op with a deprecation note — decide from the inventory: the flag no longer exists), `pyproject.toml` (`codeanalyzer-typescript==1.2.0`, `[tool.backend-versions]`), `uv.lock`
- Test: `tests/analysis/typescript/test_typescript_analysis.py`, `test_typescript_get_method_functions.py`, `test_typescript_bulk_accessors.py` (fixtures re-pointed at the v2 files), `test_typescript_backend_contract.py` (now asserts every generic-ABC method), a new e2e `test_typescript_e2e.py` that runs the real 1.2.0 binary on `tests/resources/typescript/application` and skips cleanly when absent

**Interfaces:**
- `TSAnalysisBackend(AnalysisBackend[TSApplication, TSModule, TSType, TSCallable, TSField, str])`, `P = "TS"`, `N = "TS"`.
- `TSCodeanalyzer` builds argv from 1.2.0's CLI (`-i`, `-a <int>`, `-o`, `--cache-dir`, `--skip-tests`, `--lazy`/`--eager`; **no `--tsc-only`**), reads `analysis.json` into `TSAnalysis`, exposes `.application`. `analysis_level` maps to `-a 1..4` as integers (the wiring reference's `_level_flag()` pitfall).

- [ ] **Step 1: Failing tests** — the contract test enumerating `AnalysisBackend`'s abstract methods against both TS backends; the e2e test asserting `get_symbol_table()` non-empty and `get_call_graph()` dangling-free on the real binary.
- [ ] **Step 2: Inherit the ABC**; implement every generic method the 1.x backend already had under its old name (the 12 on the generic ABC — `get_application_view`, `get_symbol_table`, `get_call_graph`, `get_all_classes`, `get_class`, `get_all_methods_in_class`, `get_method`, `get_all_fields`, `get_method_parameters`, and the five artifact/config getters — **the artifact/config five return the shared `PyArtifact`/`PyDependency`/`PyConfigKey`/`PyConfigUseEdge`/`PyConfigRead` models unchanged, as the ABC docstring promises**; if 1.2.0's artifact layer carries a field those models lack, report it rather than widen the models).
- [ ] **Step 3: Kind-filter accessors** — `get_interfaces`/`get_enums`/`get_type_aliases`/`get_namespaces`/`get_classes` become filters over `TSModule.types` by `kind`; same return types.
- [ ] **Step 4: Remove the two raising accessors** (`get_entry_point_methods`, `get_service_entry_point_methods`) with a CHANGELOG line; 2.5b brings the working trio.
- [ ] **Step 5: Pin** `1.2.0` in both places; `uv lock`. The binary resolves from the wheel's `bin_path()`; `CODEANALYZER_TS_BIN` still overrides.
- [ ] **Step 6: Run** `tests/analysis/typescript/` offline and the e2e against the real binary. Expected: all previously-green tests green on the v2 fixtures; e2e green.
- [ ] **Step 7: Commit** — `feat(typescript): inherit the generic backend ABC and drive codeanalyzer-typescript 1.2.0`.

---

## Task 3: `TSNeo4jBackend` on the 1.2.0 vocabulary

> Refined from `v1.2.0:schema.neo4j.json` (byte-identical to the release asset `schema.json` — the workflow regenerates it) and the `main` deltas.

**The contract the backend targets (1.2.0), and what `main` changes:**

- Labels, merge label, key: `TSApplication` (merge `Application`, key `id`; props `id, schema_version, language, max_level, k_limit, analyzer_name, analyzer_version`); `Artifact`/`Package`/`ConfigKey` (own merge labels, `id`); `TSDecorator` (merge `TSDecorator`, **key `name`**); and under merge label **`CanNode`** keyed `id`: `TSModule` (`name` **holds the file key**; `is_tsx, is_declaration_file, content_hash, start_line, end_line`), `TSClass`/`TSInterface`/`TSEnum`/`TSTypeAlias`/`TSNamespace` (`signature, name, …, code, start_line, end_line`), `TSCallable` (all seven callable kinds collapse onto it; an anonymous callable carries `:TSCallable:TSAnonymousCallable`), `TSField`, `TSBodyNode` (`kind, of, parent, callee, start_line, end_line`), `TSExternal` (`name, module`). `_module` is on every `CanNode` at 1.2.0 and **gone on `main`** (#166), replaced by marker labels `TSCanNode`/`JSCanNode` with range indexes on `id`.
- Relationship types and properties: `TS_HAS_MODULE`; `HAS_ARTIFACT`; `DECLARES_DEPENDENCY{spec,kind,direct,extras,prov}`; `LOCKS{version}`; `TS_PROVIDES`; `TS_UNRESOLVED_IMPORT{prov}`; `DEFINES_CONFIG`; `TS_USES_CONFIG{prov}`; `TS_DECLARES` (module|namespace|callable → type|callable); `TS_HAS_METHOD` (class|interface → callable); `TS_HAS_FIELD` (module|class|interface|enum|namespace → field); `TS_DECORATED_BY{positional_arguments, keyword_arguments_json}`; `TS_HAS_BODY_NODE`; `TS_RESOLVES_TO` (body node → callable|external|anonymous); `TS_CALLS{weight, prov}`; `TS_CFG_NEXT{kind,_k}`; `TS_CDG`; `TS_DDG{var,prov,_k}`; `TS_SUMMARY{var}`; `TS_PARAM_IN{var}`; `TS_PARAM_OUT{var}`; `TS_EXTENDS`; `TS_IMPLEMENTS`. No relationship type is added, removed or renamed between 1.2.0 and `main`.
- Constraints: uniqueness on `Application.id`, `Artifact.id`, `Package.id`, `TSDecorator.name`, `ConfigKey.id`, **`CanNode.id`**. Indexes: `TSCallable.name`, fulltext `TSCallable.code`, `CanNode.kind`, `CanNode._module` (1.2.0) → `TSCanNode.id`/`JSCanNode.id` (`main`).
- **Seek label — measured on 7690 before deciding (median of 5):** point lookup by signature: `TSCallable` 11.5 ms vs `CanNode:TSCallable` 10.8 ms, **both `NodeByLabelScan`**; module-prefix lookup (`…/src/`, 6,386 of 11,085 callables): `TSCallable` 11.4 ms scan vs `CanNode:TSCallable` **21.7 ms** `NodeUniqueIndexSeekByRange` — the seek is *slower* on a broad prefix; application-wide scan 14.3 vs 13.4 ms. `:TSCallable` has 11,085 nodes, so label scans are cheap at this scale; the 1.6 problem (955,961 `:PyCanNode`) does not exist here. **Decision, per label (both measured on 7690):** `:TSCallable` statements anchor on the specific label, **no `:CanNode`** (11,085 nodes; scans ~11 ms; the seek was 2× slower on a broad prefix). `:TSBodyNode` statements with a **narrow prefix** (one callable's body: `…/callable@`, or one module: `…/src/components/`) **do** anchor on `:CanNode:TSBodyNode` — 41.3 ms → **2.67 ms** (15×) for a callable's body nodes, 42.8 → **5.94 ms** (7×) for a module's — `NodeUniqueIndexSeekByRange` on `cannode_id`, which exists on 1.2.0 and `main` alike. Application-wide body-node scans gain nothing from the label (46–61 ms either way). **Write the two-prefix scope as `x.id STARTS WITH $p1 OR x.id STARTS WITH $p2`, never `any(p IN $prefixes WHERE …)`** — measured: with `:CanNode` the `OR` form plans as `Union(NodeUniqueIndexSeekByRange × 2)` while `any()` still plans a `NodeByLabelScan`; keep `_scope_prefixes` as the Python-side list and bind `$p1`/`$p2` from it (a helper that returns the predicate string and the params, used by every scoped statement, so the spelling cannot drift).
- **Both TypeScript live test files WRITE the graph** (`test_typescript_neo4j_backend.py`, `test_typescript_bulk_parity_live.py`): an out-of-band `_populate_neo4j` runs `codeanalyzer-typescript --emit neo4j` over Bolt, defaulting to `bolt://localhost:7687` / `neo4j` / `neo4j` when the env is unset — the #324 shape, on the port that is an ssh tunnel on the development machine. **T3 Step 0: gate both on `CLDK_TEST_NEO4J_WRITE_URI` / `_WRITE_USER` / `_WRITE_PASSWORD` with no defaults, a skip reason that says they write, and a teardown scoped to the application's two id prefixes — before running anything live.** The read-only live tests for 7690 go in new modules gated on the ordinary `CLDK_TEST_NEO4J_*` variables.
- **Mapping from the 0.4.3 vocabulary the backend queries today:** `:Application {name}` → `:Application {id: $app_id}`; `:Module {file_key}` → `:TSModule` under `TS_HAS_MODULE`, file key in `name`; `:Symbol {signature}`/`:Callable` → `:TSCallable {signature}`; `:Class`/`:Interface`/`:Enum`/`:TypeAlias`/`:Namespace` → their `TS`-prefixed labels under `TS_DECLARES`; `:Attribute`/`:Variable`/`DECLARES_VAR` → `:TSField` under `TS_HAS_FIELD`; `:CallSite`/`HAS_CALLSITE` → `:TSBodyNode {kind:'call'}` under `TS_HAS_BODY_NODE`; `:CALLS` → `TS_CALLS`; `RESOLVES_TO` → `TS_RESOLVES_TO`; `:Decorator`/`DECORATED_BY` → `TSDecorator`/`TS_DECORATED_BY`; `:External` → `TSExternal`; `:IMPORTS`/`:RE_EXPORTS` → **no equivalent** (imports/exports live only in `analysis.json`; the accessors that read them from the graph return the documented lossiness or are answered from `TSModule.imports` where the projection carries it — check which). Every `s._module IN $mods` (30 sites) and `m.file_key` join (17 sites) goes.
- Scoping: `$prefixes = ["can://typescript/<app>/", "can://javascript/<app>/"]` (TS-3), `/`-terminated exactly as `main`'s own purge does; the application id itself is `can://typescript/<app>` on both namespaces.

**Files:**
- Modify: `cldk/analysis/typescript/neo4j/neo4j_backend.py` (every statement), `cldk/analysis/typescript/neo4j/reconstruct.py`, `cldk/analysis/typescript/neo4j/config.py`
- Test: `tests/analysis/typescript/test_typescript_neo4j_bulk.py` (query-shape pins re-pointed), `test_typescript_neo4j_selection.py`, a new `test_typescript_neo4j_multi_application_scope.py` in the leg-1.6 shape, a new `test_typescript_schema_probe.py`

**Interfaces:**
- `_scope_prefixes: list[str]` = `[f"can://typescript/{app}/", f"can://javascript/{app}/"]` (TS-3); every scope predicate is `any(p IN $prefixes WHERE n.id STARTS WITH p)`; per-module narrowing uses per-module prefixes as Python does.
- `_probe_schema` (G5): required relationship types `TS_HAS_MODULE`, `TS_HAS_METHOD`, `TS_HAS_BODY_NODE`, `TS_CALLS`; `MATCH (a:Application {id: $app_id})` present; `analyzer_version` parsed, `< 1.2.0` or absent → `GraphSchemaMismatch` naming found and floor; records `_analyzer_version`.
- `module_key_of(node_id, prefix, known)` with the TS known-key set (`.ts/.tsx/.js/.jsx/.mjs/.cjs` keys as the graph carries them).

- [ ] **Step 1: Failing tests first** — the multi-application audit (class-level **and** inline statements, the harvesting rule from leg 1.6), a fake two-application graph in the **1.2.0 vocabulary carrying no `_module`**, the probe matrix (`0.4.3`, `1.1.0`, `1.2.0`, absent application, garbage), selection-by-config. Expected: audit red on every `:Symbol`/`:CALLS`/`_module` statement.
- [ ] **Step 2: Re-point statement by statement** — `:Application {name}` → `:Application {id}`; `:Module {file_key}` → `:TSModule` under `TS_HAS_MODULE`; `:Symbol {signature}`/`:Callable` → `:TSCallable`; `:Class` → `:TSClass` (and `TSInterface`/`TSEnum`/`TSTypeAlias`/`TSNamespace` for the kind-filter accessors); `:CallSite`/`HAS_CALLSITE` → `:TSBodyNode {kind:'call'}`/`TS_HAS_BODY_NODE`; `:CALLS` → `TS_CALLS {prov, weight}`; `RESOLVES_TO` → `TS_RESOLVES_TO`; `:Decorator`/`DECORATED_BY` → `TSDecorator`/`TS_DECORATED_BY`; `:External` → `TSExternal`; `DECLARES_VAR`/`:Variable` → `TS_HAS_FIELD`/`TSField`. Every `_module IN $mods` → the two-prefix predicate. Keep the N+1 discipline leg 1.5 established: bulk `collect()` fetches per parent kind, no per-child round trips.
- [ ] **Step 3: `reconstruct.py`** rebuilds `TSApplication` from the rows into the T1 models — same lossiness statements as Python's (`code` derived; body text absent for value vertices).
- [ ] **Step 4: Live parity on 7690** for every accessor that exists after T2: symbol-table key set, class/interface/enum sets, call-graph node and edge counts within the documented tolerances, the four bulk accessors, `get_external_symbols`; and the dangling-free `get_call_graph()`.
- [ ] **Step 5: Commit** — `feat(typescript): Neo4j backend on the codeanalyzer-typescript 1.2.0 vocabulary, scoped by can:// prefix`.

---

## Task 4: Live suite, audit, docs

- [ ] **Step 1: Three runs, sequentially, verbatim** — exact release gate (floor 686/187), Python live on 7689 (floor 590), TypeScript live on 7690 (new baseline; record it). Then `tests/analysis/typescript/` offline count vs the 101 floor.
- [ ] **Step 2: `grep -rn "_module\|tsc.only\|:Symbol\|HAS_CALLSITE" cldk/analysis/typescript cldk/models/typescript`** — only prose explaining history may remain.
- [ ] **Step 3: Refusals** — attaching to a 0.4.3-shaped fake raises `GraphSchemaMismatch`; attaching to 7690 as `superset-frontend-b` raises it; attaching to 7689 (a Python graph) raises it naming the missing `TS_*` types.
- [ ] **Step 4: Docs** — `CHANGELOG.md` `[Unreleased]`: pin `0.4.3 → 1.2.0`, refused-graph behaviour, the two removed raising accessors, the `commons/` lift (internal); `docs/agent-api-reference.md`: a TypeScript section stating what attaches today, the version floor, JavaScript scope, and that the leg-1.5/1.6 query surface arrives in 2.5b; `CLAUDE.md` supported-languages row for TypeScript (pin and backends unchanged in shape). Plan checkboxes ticked with "as done" notes where reality differed.
- [ ] **Step 5: Commit** — `docs(typescript): record the schema-v2 migration and the 1.2.0 pin`.

## Definition of done

- Spec §8's 2.5a half: every existing TypeScript accessor green on the live superset graph; `python/backend.py` retains only Python-shaped code with Python's suite unchanged in count; the TS multi-application audit enumerates every statement; refusals as above; the CHANGELOG and reference updated.
- One PR to `release/2.0`, `Closes #335`.

## Not in this plan

The query surface (2.5b); the pin to 1.3.0; entrypoints; JavaScript-specific semantics beyond the second prefix; cants #171/#172/#94/#95 (upstream).

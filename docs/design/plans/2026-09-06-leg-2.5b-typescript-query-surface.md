# Leg 2.5b — the TypeScript query surface Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give TypeScript the agent-facing query surface Python gained in legs 1.5 and 1.6 — addressing, per-callable graphs, bounded slices and flows, entrypoints, and the artifact/config layer — on both backends, on codeanalyzer-typescript 1.3.0, with every pre-existing accessor unmoved.

**Architecture:** Four moves. (T0) `LocateResult` goes language-neutral (TS-1) so `commons/results.py` stops importing `cldk.models.python`, and the pin moves to 1.3.0 — a change that must alter no model, since 2.5a pre-declared 1.3.0's additive fields. (T1) The addressing surface on both backends: `locate`, `locate_many`, `resolve_callable`, `resolve_value`, `get_source`, `describe`, `has_resolution_edges`, plus the scoping keywords on the existing accessors. (T2) The dataflow surface: per-callable `EdgePage`s, slices, reachability, paths, flow predicates — Python's bounds and completeness protocol, no new rulings. (T3) Entrypoints, the artifact/config layer on the facade, the multi-application audit over every new statement, docs.

**Tech Stack:** Python 3.11+, pydantic v2, neo4j driver 5.x, codeanalyzer-typescript 1.3.0 (bun-compiled binary in the PyPI wheel; `--emit neo4j` is always full depth).

**Spec:** `docs/design/specs/2026-09-06-leg-2.5-typescript.md` (TS-1, TS-2, TS-3, and the 2.5b half of TS-4). Tracking: python-sdk#343 under codellm-devkit/.github#55.

## Global Constraints

- **Nothing pre-existing moves.** `tests/analysis/typescript/test_typescript_public_surface.py` freezes 46 accessors' signatures; this leg's frozen list grows by exactly what it adds and changes no existing entry. Any other diff is a defect.
- **Python's semantics are the contract, not a starting point.** Every accessor this leg adds must match `cldk/analysis/python/python_analysis.py`'s signature and semantics exactly, including keyword-only-ness, defaults, and which bound is finite. The exact signatures to mirror are in each task. Where TypeScript genuinely cannot answer, it raises naming the gap — never an empty that reads as a fact (D7).
- **Bounds are never silent (E5).** Slices and cones default `depth` to `DEFAULT_DEPTH` and cap `max_nodes`; `reaches`, `paths_between`, `call_paths_between`, `flows_to_call` and `flows_to_argument` are **unbounded by default** (`depth=None`) because a bounded predicate returns a wrong answer, not a small one. One completeness protocol: the `complete` field on `EdgePage`/`Slice`/`FlowPaths`.
- **No `can://` and no ordinals** in any signature, in any return field other than `ref`/`node_id`/`next_cursor`, or in any error message (E6/E7). Errors name what missed; no suggestions, no fuzzy matching, anywhere, including error paths (E8).
- **Two prefixes (TS-3).** Application scope stays the list `can://typescript/<app>/` and `can://javascript/<app>/`, spelled through the existing `_scoped()` helper as `$p1 OR $p2` — never `any()`, which measurably defeats the index seek.
- **Seek labels are measured, not assumed.** 2.5a's measured rule: bare label for signature and prefix statements, `:CanNode:<Label>` only for id-equality point lookups. Re-measure for any new statement family (`PROFILE`, median of 5) and record the numbers in this file. Java's leg 3a found the opposite ruling on its own graph; do not port a rule across languages.
- **Read-only against every Neo4j instance.** Reference graph: `bolt://localhost:7692`, user `neo4j`, password `cldkleg25btest`, application `superset-frontend`, emitted by the **released 1.3.0 wheel** (154,451 nodes / 449,394 relationships; anchor stamps `analyzer_version 1.3.0`; `k_limit 3`). The 2.5a graph on 7690 stamps `1.2.0` and is kept as the below-floor refusal fixture. Python regression graph: `bolt://localhost:7689` / `cldkleg16test` / `odoo-slim-19`. **Port 7687 is an ssh tunnel; never a target.** Containers are in the `podman` docker context. The two TypeScript live-writer files stay gated on `CLDK_TEST_NEO4J_WRITE_*`; never set those.
- Run suites **sequentially**. Stage by name. Worktree `<scratchpad>/wt-leg25b` (branch `feat/issue-343-ts-query-surface`), stacked on 2.5a's head `0170f67`; rebase onto `release/2.0` once #342 merges.
- **Baselines at `0170f67`:** release gate **883 passed / 212 skipped**, coverage 79.28%; TypeScript live on 7690 **274 passed / 24 skipped**; models 25; contracts and frozen surface 112. **After Task 0:** release gate **893 passed / 212 skipped** at 79.32%; TypeScript live moves to 7692 (7690 is below the floor and refused) at **277 passed / 24 skipped**; models 28; Python live on 7689 **588 passed / 6 skipped** (the plan-era figure of 590 does not reproduce and is stale).
- **Never add Claude/AI attribution** to any commit, comment, doc or changelog entry.

## What the 1.3.0 graph actually holds (measured on 7692, authority for every task)

- Anchor `(:Application:TSApplication {id: "can://typescript/superset-frontend"})` with `schema_version 2.0.0`, `language typescript`, `max_level 4`, `k_limit 3`, `analyzer_version 1.3.0`, **`entrypoint_report_json`** (a JSON string, not structured nodes) and `entrypoint_frameworks: ["commander"]`.
- Labels: `CanNode` 150,443 (the unprefixed merge label; cants#94/#95 would prefix it), marker labels `TSCanNode` 117,048 and `JSCanNode` 33,396, `TSBodyNode` 125,532, `TSCallable` 11,085 (of which `TSAnonymousCallable` 7,044), `TSField` 6,951, `TSExternal` 3,171, `Package` 2,649, `TSModule` 1,841, `Artifact` 832, `TSTypeAlias` 815, `TSInterface` 687, `ConfigKey` 526, `TSClass` 207, `TSEnum` 108, `TSNamespace` 49.
- Relationships: `TS_HAS_BODY_NODE` 125,532, `TS_DDG` 119,384 (**`prov` is `["reaching-defs"]` on every edge** — TypeScript has no `ssa`/`points-to` tier, unlike Python's three), `TS_CFG_NEXT` 62,087, `TS_CDG` 32,387, `TS_SUMMARY` 20,316, `TS_CALLS` 17,712, `TS_RESOLVES_TO` 16,324, `TS_DECLARES` 11,990, `TS_PARAM_IN` 11,047, `LOCKS` 7,617, `TS_PARAM_OUT` 7,428, `TS_HAS_FIELD` 6,951, `DECLARES_DEPENDENCY` 3,601, `TS_PROVIDES` 2,696, `TS_HAS_MODULE` 1,841, `TS_HAS_METHOD` 959, `HAS_ARTIFACT` 832, `DEFINES_CONFIG` 526, `TS_EXTENDS` 154, `TS_UNRESOLVED_IMPORT` 10.
- **Absent from this corpus, still:** `TS_IMPLEMENTS`, `TS_DECORATED_BY`, `TS_USES_CONFIG`, and any `TSDecorator` node. That is a property of superset-frontend, not of 1.3.0 — 2.5a's "untested by corpus" wording stays true, and `get_config_uses` still cannot distinguish "no config uses" from "vocabulary absent".
- `_module` is on **0** nodes; body-node ids are present on **all** 125,532 (`TS_HAS_BODY_NODE` children are addressable, which is what makes the per-callable graphs and slices possible at all).

---

## Task 0: `LocateResult` goes language-neutral; the pin moves to 1.3.0

**Files:**
- Modify: `cldk/analysis/commons/results.py` (drop the `cldk.models.python` import; add `BodyRef`), `cldk/analysis/python/backend.py` and `cldk/analysis/python/neo4j/neo4j_backend.py` (populate `BodyRef` where they populated the Python body node), `pyproject.toml` (`codeanalyzer-typescript==1.3.0` in `dependencies` and `[tool.backend-versions]`), `uv.lock`
- Test: `tests/analysis/commons/test_lifted_helpers.py` (assert `commons/` imports no language package), `tests/analysis/typescript/test_typescript_schema_probe.py` (floor moves to 1.3.0), `tests/models/typescript/test_ts_v2_models.py` (a 1.3.0 payload parses unchanged)

**Interfaces:**
- Produces: `BodyRef{id: str, kind: str, span: Span | None, callee: str | None}` in `cldk/analysis/commons/results.py`, and `LocateResult.body` retyped to `BodyRef | None`. `Span` moves with it — it is the shared span type both analyzers emit, and leaving it in `cldk/models/python` is what forces the cross-language import. Keep `Span`'s class identity and re-export it from `cldk.models.python` so no Python caller notices.
- Consumes: nothing.

- [x] **Step 1: Failing test** — assert that no module under `cldk/analysis/commons/` imports from `cldk.models.python`, `cldk.models.typescript` or `codeanalyzer*`, by parsing each module's AST (the runtime check cannot work: importing `cldk` loads the whole SDK). Assert `BodyRef` is importable from `cldk.analysis.commons.results` and that `LocateResult.model_fields["body"]` is typed `BodyRef | None`.
- [x] **Step 2: Run it; watch it fail on the current `from cldk.models.python import BodyNode, Span`.**
- [x] **Step 3: ~~Move~~ *add* `Span` and add `BodyRef`.** `BodyRef{id, kind, span, callee}` and `Span{start, end, bytes}` are new in `cldk/analysis/commons/results.py`; `LocateResult.node: BodyNode | None` became `LocateResult.body: BodyRef | None`; both Python backends map their body node onto it where they build the result, and `as_slice_node` reads `.body`.
  - *as done:* four tests in `tests/analysis/commons/test_lifted_helpers.py`. **The blanket assertion as written is wrong and was narrowed:** `commons/backend.py` declares the repository-artifact getters on `PyArtifact`/`PyConfigKey`/`PyConfigRead`/`PyConfigUseEdge`/`PyDependency`, which its own module docstring documents as the one cross-language layer every analyzer projects identically — and Task 3's signatures keep them. So the test allow-lists exactly those five names and fails on anything else; `commons/artifacts.py`'s `Span` import went away by spelling the span as a mapping, so the allow-list needs no second category. The leg's DoD line "`cldk/analysis/commons/` imports no language package" is unachievable as stated and should read "imports no language package beyond the shared artifact layer".
  - *as done:* 3 failed / 7 passed — the import assertion, the `BodyRef` field-shape assertion, and the span-coercion assertion. The fourth (Python still re-exports its analyzer's `Span` object) passed then and passes now, which is the point of it.
- [x] **Step 4: Move the pin to 1.3.0** in both places; `uv lock`; `uv sync --all-groups --all-extras`. Raise the probe's floor to `(1,3,0)` and update the refusal test matrix: a 1.2.0 graph (7690) is now **below floor** and must be refused, which is the reason 7690 is kept.
  - *as done, and the plan was wrong here:* **`Span` cannot be moved and keep its class identity.** It is not defined in `cldk/models/python` — that package re-exports `codeanalyzer.schema.py_schema`, and every `Py*` model's `span` field is annotated on *that* class. Re-exporting a commons class in its place makes `BodyNode(span=Span(...))` a `ValidationError` (measured), which is what `commons/artifacts.py`, `python/neo4j/reconstruct.py`, `python/neo4j/neo4j_backend.py`, `python/codeanalyzer/codeanalyzer.py` and two test modules all do — so the "move" drags the whole Python schema, or else silently breaks `isinstance(callable.span, Span)` for every Python caller. Instead the commons `Span` is **added** alongside, with `model_config = ConfigDict(from_attributes=True)`: both `codeanalyzer-python`'s `Span` and `cldk.models.typescript`'s `TSSpan` validate into it field-for-field, so no backend converts by hand, and `cldk.models.python.Span` stays the analyzer's own object (pinned by an `is`-identity test against `codeanalyzer.schema.py_schema.Span`).
- [x] **Step 5: Prove the bump moves no model.** Parse the 1.3.0 fixture (regenerate it with the 1.3.0 wheel, same command the v2 README records) and assert `TSAnalysis.model_validate_json` succeeds with `extra="forbid"`, that `entrypoint_report` is present and required-satisfied, and that `is_entrypoint` parses on all five type kinds. If any model needs a field, that is a 2.5a omission — say so in the report rather than widening quietly.
- [x] **Step 6: Run** `tests/analysis/commons tests/models/typescript tests/models/python`, then the Python live suite on 7689 (floor 588/6 — `Span` moved, so Python is the regression gate), then the release gate. Every count explained.
  - *as done:* `pyproject.toml` `dependencies` + `[tool.backend-versions]`, `uv.lock` (`1.2.0 -> 1.3.0`, nothing else moved). `_ANALYZER_FLOOR = (1, 3, 0)`. The floor test is parametrized over `1.1.0` / `1.2.0` / `1.2.9`, plus a new test asserting the 1.2.0 refusal is credited to the version and not to a missing relationship type (`e.value.missing == set()`). **Verified live:** 7692 attaches at `(1,3,0)`; 7690 raises `GraphSchemaMismatch: … was emitted by codeanalyzer-typescript 1.2.0; this backend needs a graph emitted by codeanalyzer-typescript 1.3.0 or newer.` The two live tests that had the old floor written down now read it off `TSNeo4jBackend._ANALYZER_FLOOR`.
- [x] **Step 7: Commit** — `refactor(commons): language-neutral BodyRef and Span; pin codeanalyzer-typescript 1.3.0`.
  - *as done:* all four fixtures regenerated with the 1.3.0 wheel (same command, `--app-name slim`, `-a 1..4`), kept in place, README version line updated with the measured diff. **No model moved.** All four parse under `extra="forbid"`. Two qualifications on the step's own wording: `entrypoint_report` is *present* in all four but stays **optional** on `TSApplication` (the graph-backed application view carries the report as `entrypoint_report_json` on the anchor, not as a structured field, so making it required would break that path) — that is a deliberate divergence from 1.3.0's `schema.ts`, not an omission; and `is_entrypoint` is *declared* on all five type kinds (it is on `TSType`, the base) but only **classes and callables** carry it in this corpus — the other four kinds keep the model's `None` default and stay untested by corpus. One unadvertised analyzer change: 1.3.0 emits **no** `decorators[].qualified_name` at all (1.2.0 emitted it, equal to `name`); the field was already `Optional[str] = None`, which is exactly why nothing had to be widened, and no TypeScript accessor reads it.
  - *as done:* targeted **40 passed** (commons 6→10, models/typescript 25→28, models/python 2). Python live on 7689 **588 passed / 6 skipped** — the baseline exactly, zero delta (the run was `tests/analysis/python tests/analysis/commons tests/models/python` = 600/6; subtract the 12 non-`tests/analysis/python` tests). Release gate **893 passed / 212 skipped**, coverage **79.32%** (baseline 883/212 at 79.28%): +4 commons TS-1 tests, +3 probe tests, +3 model tests, no skip moved. TypeScript live re-pointed at 7692 (the 1.3.0 graph): **277 passed / 24 skipped** — 2.5a's whole suite green on 1.3.0 unchanged, +3 being the new probe tests.

---

## Task 1: the addressing surface

**Files:**
- Modify: `cldk/analysis/typescript/backend.py` (six new abstract methods), `cldk/analysis/typescript/codeanalyzer/codeanalyzer.py`, `cldk/analysis/typescript/neo4j/neo4j_backend.py`, `cldk/analysis/typescript/typescript_analysis.py`
- Test: new `tests/analysis/typescript/test_typescript_addressing.py` (offline, over the v2 fixtures), new `tests/analysis/typescript/test_typescript_addressing_live.py` (7692, gated on `CLDK_TEST_NEO4J_*`), `test_typescript_public_surface.py` (frozen list grows)

**Interfaces** — mirror these exactly (from `cldk/analysis/python/python_analysis.py`):
```
locate(self, path: str, line: int) -> LocateResult
locate_many(self, positions: Sequence[Tuple[str, int]]) -> List[LocateResult]
resolve_callable(self, name: str, *, in_class: str | None = None, in_module: str | None = None) -> SliceNode
resolve_value(self, name: str, *, within: str) -> SliceNode
get_source(self, node_id: str) -> str
describe(self, nodes: Sequence[object]) -> List[SliceNode]
has_resolution_edges(self) -> bool
```
Consumes from T0: `BodyRef`, `Span`. Produces for T2: `resolve_callable`/`resolve_value` are how every slice and flow accessor turns a caller's name into an id, so their raise behaviour is T2's contract too.

- [x] **Step 1: Failing tests, offline first.** `locate("src/controllers.ts", <line>)` returns the module, the enclosing type and callable, and a `BodyRef` for the statement at that line; `locate` off the end of a file returns the module with `callable=None` rather than raising; `resolve_callable("Controller.handle")` returns a `SliceNode`; an ambiguous simple name raises `AmbiguousName` **listing the candidates and nothing else** (E8 — no "did you mean"); a name in no module raises `SelectorNotInGraph` naming the selector; `resolve_value("x", within=<callable>)` resolves a local; `get_source(<node id>)` returns the exact slice; `describe` accepts ids, `SliceNode`s and overviews and returns `SliceNode`s.
  - *as done:* 29 tests in `tests/analysis/typescript/test_typescript_addressing.py`, over the a4/a2/a1 fixtures of the tracked sample app. Two of the step's own examples were wrong against the corpus and were replaced by what it holds: there is no `Controller.handle`, so the worked example is `UserController.show` (`src/controllers.ts:18-22`), and `resolve_value` resolves a **parameter**, not "a local" — `var`/`of` is non-null only on the four parameter-passing kinds, exactly as in Python, so a local that is only assigned and read has no address here at all. `describe` takes `SliceNode`s and `LocateResult`s; it does **not** take a `TSCallableOverview`, which carries no `ref`/`node_id` (`as_slice_node` raises `TypeError`) — the same as Python, whose docstring's "overviews" wording is likewise aspirational.
- [x] **Step 2: Run them; watch them fail on `AttributeError`.**
  - *as done:* 29 failed, every one on `AttributeError: 'TypeScriptAnalysis' object has no attribute …`.
- [x] **Step 3: Implement on `TSCodeanalyzer`** over the in-memory tree. Module keys resolve by the verified longest suffix match `resolve_module_key` already does — never by splitting on an extension (F4), and the known-key set spans both prefixes' extensions (`.ts/.tsx/.js/.jsx/.mjs/.cjs`).
  - *as done:* `cldk/analysis/typescript/codeanalyzer/codeanalyzer.py:718-922`. `resolve_module_key` does the key matching, unchanged. Two things the step did not anticipate. (a) **A body-node id cannot be split on `@`**: an anonymous callable's own id contains one (`…/<anon@22:52>@23:7`), so `get_source` looks the id up whole through `_sources_for` rather than partitioning it the way the Python backend does — the same reason `describe` and `get_source` share one seam here. (b) **`TSBodyNode.id` is read, never composed**: 1.3.0 emits it on all 125,532 body nodes (cants#165), and a span-bearing node without one raises naming the callable rather than being composed around.
- [x] **Step 4: Implement on `TSNeo4jBackend`.** Measure the seek label for the two new statement families — a `(path, line)` lookup and a name lookup — and record the numbers here before choosing an anchor. Both prefixes, one `_scoped()` spelling.
  - *as done:* `cldk/analysis/typescript/neo4j/neo4j_backend.py:895-1229`, anchors chosen from the table below. Three facts the step did not have. (a) The graph's `:TSBodyNode` carries **`callee` as a property**, so `BodyRef.callee` is populated over Neo4j too — one place TypeScript is *less* lossy than the Python twin, where callee resolution is a separate `PY_RESOLVES_TO` hop and the field is always `None`. (b) `locate_many` never binds a module prefix for a key the application does not hold: the raw path is not application-stamped and would be a seek outside the application, and it has no rows either way. (c) The declaration-merge guard is a `kind IN $callable_kinds` predicate alongside the label, the same "kind, not label" rule `_types_by_signature` already used.

  **Measured on 7692 (superset-frontend, 1.3.0), `PROFILE`-shaped statements timed end-to-end over
  the driver, median of 5 with the first discarded, two independent runs.** The two families land on
  *opposite* anchors, so 2.5a's "bare label for prefix statements" is refined rather than repeated:
  what decides it is how narrow the prefix is, not that there is one.

  | statement family | anchor | run 1 | run 2 | rows | chosen |
  |---|---|---|---|---|---|
  | `locate_many`, 40 positions (per-**module** id prefix) | `(c:TSCallable)` | 357.9 ms | 346.2 ms | 113 | |
  | | `(c:CanNode:TSCallable)` | **97.0 ms** | **102.2 ms** | 113 | ✅ 3.5× |
  | | `(c:TSCanNode:TSCallable)` | 37.9 ms | 35.5 ms | **40** | ✗ wrong answer |
  | `resolve_callable("render")` (per-**application** id prefix ×2) | `(c:TSCallable)` | **27.6 ms** | **26.8 ms** | 86 | ✅ |
  | | `(c:CanNode:TSCallable)` | 45.6 ms | — | 86 | |
  | `resolve_callable("onChange")` | `(c:TSCallable)` | **26.0 ms** | — | 19 | ✅ |
  | | `(c:CanNode:TSCallable)` | 50.6 ms | — | 19 | |
  | `resolve_callable("getAbsolutePath")` | `(c:TSCallable)` | **24.4 ms** | — | 2 | ✅ |
  | | `(c:CanNode:TSCallable)` | 43.8 ms | — | 2 | |
  | `resolve_callable("<anon@22:52>")` | `(c:TSCallable)` | **28.3 ms** | — | 1 | ✅ |
  | | `(c:CanNode:TSCallable)` | 46.1 ms | — | 1 | |

  A module prefix selects tens of nodes out of 150,443, so seeking `cannode_id` beats scanning the
  11,085-node `:TSCallable` label; an application prefix selects nearly all of them, so the same seek
  is a full range walk and the label scan wins — exactly 2.5a's reason for the bare label, holding for
  the same reason on a different family. `:TSCanNode` is refused on **correctness**, not cost: it is
  the per-namespace marker, so it drops every `can://javascript/<app>/` module (40 rows where the
  right answer is 113). Java's leg-3a ruling is not ported; neither is 2.5a's, unexamined.
- [x] **Step 5: The dotted vocabulary is TypeScript's, not Python's.** `in_module=` accepts a module key suffix or the dotted form `module_dotted(path, extensions=(".ts",".tsx",".js",".jsx",".mjs",".cjs"))` — note that helper's `/__init__` strip is Python-specific and documented as such; confirm it cannot fire on a TypeScript path, and if it can, pass the behaviour as a parameter rather than living with it.
  - *as done, and the step's own guess was wrong:* the `/__init__` strip **can** fire on a TypeScript path — nothing stops a project having `src/foo/__init__.ts`, which is a module in its own right and must dot to `src.foo.__init__`. (superset-frontend has none, so it fires nowhere on the reference corpus; the risk is a corpus that does.) So it is now a parameter: `module_dotted(path, *, extensions, package_index="__init__")` in `cldk/analysis/commons/keys.py:142`, and `cldk/analysis/typescript/backend.py:95` binds `ts_module_dotted = partial(module_dotted, extensions=TS_EXTENSIONS, package_index=None)`. `resolve_callable_signature` takes the bound function as one `dotted=` parameter rather than forwarding two knobs. TypeScript's own `index.ts` convention is deliberately **not** stripped either: `src/foo/index.ts` is addressed by that key everywhere else on this surface, so it dots to `src.foo.index`.
- [x] **Step 6: Backend parity test** — every accessor answers identically on both backends over the same application, including the miss paths, and raises the same exception type with the same subject named.
  - *as done, with one honest gap:* 8 parity tests in `tests/analysis/typescript/test_typescript_bulk_parity_live.py` — `locate_many` over every callable and every module of the sample app, `resolve_callable` over every callable plus its scoping keywords, `resolve_value` over every entering value (against a level-4 local reference of its own, since `formal_in` first exists at level 4 while `--emit neo4j` is always full depth), `get_source`, `describe`, `has_resolution_edges`, and a `_same_raise` helper asserting both backends raise the **same exception type naming the same subject** on every miss path. **That module writes the graph and is gated on `CLDK_TEST_NEO4J_WRITE_*`, which this leg's brief forbids setting, so these 8 did not run here — they are written, they collect, and they skip.** What did run: the Neo4j half at scale on 7692 (26 tests) and offline over a two-application fake graph whose Cypher is really evaluated (14 tests, including a shared module key `src/index.ts` declared by both applications), and the local half over the fixtures (29 tests). The dual-backend claim is therefore REASONED, not VERIFIED.
- [x] **Step 7: Run** `tests/analysis/typescript` offline, then live on 7692. Commit — `feat(typescript): addressing surface — locate, resolve, source, describe`.
  - *as done:* TypeScript offline **313 passed / 82 skipped**; TypeScript live on 7692 **363 passed / 32 skipped** (baseline 277/24; the +8 skips are the write-gated parity module); release gate **953 passed / 246 skipped** at **79.86%** (baseline 893/212 at 79.32%); Python live on 7689 **588 passed / 6 skipped**, the baseline exactly. Every delta accounted for in the report.

---

## Task 2: per-callable graphs, slices, reachability, paths, flow predicates

**Files:**
- Modify: `cldk/analysis/typescript/backend.py`, both backends, `cldk/analysis/typescript/typescript_analysis.py`
- Test: new `tests/analysis/typescript/test_typescript_dataflow.py`, new `tests/analysis/typescript/test_typescript_dataflow_live.py`, `test_typescript_public_surface.py`

**Interfaces** — mirror exactly:
```
get_cfg(self, callable: str, *, in_class: str | None = None, page_size: int = DEFAULT_PAGE_SIZE, cursor: str | None = None) -> EdgePage[CfgEdge]
get_cdg(self, callable: str, *, in_class: str | None = None, page_size: int = DEFAULT_PAGE_SIZE, cursor: str | None = None) -> EdgePage[CdgEdge]
get_ddg(self, callable: str, *, in_class: str | None = None, page_size: int = DEFAULT_PAGE_SIZE, cursor: str | None = None) -> EdgePage[DdgEdge]
slice_backward(self, src: str, *, within: str, depth: int | None = DEFAULT_DEPTH, max_nodes: int = DEFAULT_MAX_NODES) -> Slice
slice_forward(self, src: str, *, within: str, depth: int | None = DEFAULT_DEPTH, max_nodes: int = DEFAULT_MAX_NODES) -> Slice
backward_cone(self, sinks: Sequence[str], *, depth: int | None = DEFAULT_DEPTH, max_nodes: int = DEFAULT_MAX_NODES) -> Slice
reaches(self, src: str, dst: str, *, depth: int | None = None) -> bool
callers_of(self, name: str, *, in_class: str | None = None, in_module: str | None = None) -> List[SliceNode]
callees_of(self, name: str, *, in_class: str | None = None, in_module: str | None = None) -> List[SliceNode]
paths_between(self, src: str, dst: str, *, src_within: str, dst_within: str, depth: int | None = None, max_paths: int = DEFAULT_MAX_PATHS) -> FlowPaths
call_paths_between(self, src: str, dst: str, *, depth: int | None = None, max_paths: int = DEFAULT_MAX_PATHS) -> FlowPaths
flows_to_call(self, src: str, callee: str, *, within: str, depth: int | None = None) -> bool
flows_to_argument(self, src: str, callee: str, arg: str, *, within: str, depth: int | None = None) -> bool
```
Note `paths_between` takes **`src_within` and `dst_within`**, not one `within`: a single scope can never find a cross-callable path.

- [x] **Step 1: Failing tests, offline.** Per-callable pages: the first page of a callable's CFG is complete for a small callable and `complete is False` with a `next_cursor` for a large one; paging with the cursor yields the rest and never repeats an edge; `page_size=0` and a bad cursor raise naming the argument. Slices: a backward slice from a value inside a callable; the default depth is finite and a truncated slice reports `complete is False`; `max_nodes` caps and says so. Predicates: `reaches` is unbounded by default; a `depth` that cuts a known-true path returns `False` **and** that asymmetry is the reason predicates are unbounded — assert the unbounded call is `True`. Paths: `paths_between` with the two scopes; `max_paths` truncation reported. `flows_to_argument` with a real argument position.
  - *as done:* 26 tests in `tests/analysis/typescript/test_typescript_dataflow.py`, over the a4/a2 fixtures. Every number was derived by walking the fixture's own `cfg`/`cdg`/`ddg`/`summary` lists and `param_in`/`param_out` overlays with a throwaway script, never by running the implementation: `UserController.show` has 5 CFG / 2 CDG / 9 DDG edges, and the forward closure of its `id` parameter is 3 / 13 / 23 / 52 nodes at depth 1 / 3 / 5 / unbounded. One correction to the step's own wording: `Slice` has no `truncated` attribute — the completeness protocol is `complete`, and `BoundedResult` makes it a computed field (`total == len(nodes)`).
- [x] **Step 2: Run them; watch them fail.**
  - *as done:* 26 failed, every one on `AttributeError: 'TypeScriptAnalysis' object has no attribute …`.
- [x] **Step 3: Implement on `TSCodeanalyzer`** over the in-memory `body`/`cfg`/`cdg`/`ddg`/`summary` lists, reusing `cldk/analysis/commons/{bounds,graphs}.py` — `bounded_subgraph`, `edge_sort_key`, `flow_path(..., via=via_table("TS"))`, `as_slice_node`, `cone_sinks`, `slice_resolved`, the keyset cursor codec. Nothing here is new logic; if a helper does not fit, say why rather than forking it.
  - *as done:* `cldk/analysis/typescript/codeanalyzer/codeanalyzer.py:924-1236`. Every named helper is reused except two, and neither is a fork.
    - **`bounded_subgraph` does not fit and is not used.** It is `get_call_graph(roots=, depth=)`'s helper — a *scoping keyword* on an enumeration — and no accessor in this task takes `roots=`. `backward_cone` walks backwards (it needs `nx.ancestors` / a reversed `ego_graph`), and `reaches` needs a membership test, not an induced subgraph.
    - **`body_node_kind` does not fit**, so `ts_body_node_kind` is a new five-line function in `cldk/analysis/typescript/backend.py:150`, not a copy: cants' `of` grammar shares **no token** with codeanalyzer-python's `var` grammar. A `formal_out`'s `of` is `"$ret"` where Python writes `"<return>"`; an `actual_in`'s is `"arg0"`/`"arg1"` — a *position*, where Python names the parameter — so it is reported as `name=None` rather than putting an ordinal in a return field (E7); and none of the 10,465 `formal_in` vertices carries a `<global>:`/`<capture>:` marker, so there is no tier to translate.
    - **One helper was lifted rather than forked**: `PyCodeanalyzer._shortest_walks` moved to `cldk/analysis/commons/graphs.py:193` as `shortest_walks(..., via=)`. It is a graph algorithm over an adjacency of strings and knows no language; both local backends now bind it with `partial(shortest_walks, via=VIA)`. Python live on 7689 is unchanged by the move (588/6).
- [x] **Step 4: Implement on `TSNeo4jBackend`** with `sdg_rels("TS")`/`sdg_rel_pattern("TS")`. Quantified path patterns need Neo4j 5.9+ — record the server version the probe reads and fail at the call, not at attach, if it is older. Measure the seek label for the per-callable body-node prefix family: 2.5a measured a 15× win for `:CanNode:TSBodyNode` on a narrow prefix, which is exactly this shape — confirm it on 7692 and record the numbers.
  - *as done:* `cldk/analysis/typescript/neo4j/neo4j_backend.py:1240-1552`. **Server version: the reference server on 7692 reports `5.26.30`**, read at attach by `_read_server_version` and enforced at `reaches` / `backward_cone` by `_require_quantified_paths`, never at attach — an unreadable version means *unknown* and blocks nothing.

  **The seek measurement confirmed 2.5a's narrow-prefix rule and also found a correctness bug in the obvious spelling.** Statements timed end-to-end over the driver, median of 5 with the first discarded, on 7692:

  | statement family | anchor | drawGraph (1,933 DDG) | DatabaseModal (461) | FiltersConfigForm (399) | transformProps (366) |
  |---|---|---|---|---|---|
  | per-callable page, **id-prefix** `s.id STARTS WITH <callable id>@` | `(s:TSBodyNode)` | 82.8 ms | 56.6 ms | 61.6 ms | 44.3 ms |
  | | `(s:CanNode:TSBodyNode)` | **42.8 ms** | **11.7 ms** | **10.8 ms** | **9.2 ms** ✅ |
  | per-callable page, **containment** `(c)-[:TS_HAS_BODY_NODE]->(s)-[r]->(d)<-[:TS_HAS_BODY_NODE]-(c)` | `(c:TSCallable)` | 55.5 ms | 19.4 ms | 17.6 ms | 16.8 ms |
  | | `(c:CanNode:TSCallable)` | 636.8 ms | 147.7 ms | 118.4 ms | 106.5 ms |

  | statement family | anchor | seed 1 | seed 2 | seed 3 |
  |---|---|---|---|---|
  | slice seed, id-equality point lookup, depth 5 | `(r:TSBodyNode {id})` | 42.1 ms | 42.3 ms | 39.9 ms |
  | | `(r:CanNode:TSBodyNode {id})` | **3.2 ms** | **5.6 ms** | **2.8 ms** ✅ 13× |

  A per-callable body-node prefix is the narrowest prefix on this surface, so the `CanNode.id` index earns its seek (2–6×), exactly as 2.5a predicted; the application-prefixed containment form lands on the bare label, exactly as Task 1's rule says.

  **The containment spelling is also wrong, and that is why it is not used.** It is the Python backend's `_OWN_EDGES` shape, and it binds two `TS_HAS_BODY_NODE` relationships in one `MATCH`; Cypher's relationship-uniqueness rule forbids them from being the *same* relationship, so every **self-loop** edge is silently dropped. Measured on 7692: **173 `TS_DDG` and 2,511 `TS_SUMMARY` edges run from a body node to itself**, and `drawGraph`'s DDG page came back 1,931 against the true 1,933. The id-prefix spelling has no such pattern and returns all of them. *(This is a latent defect in `PyNeo4jBackend.get_cfg/get_cdg/get_ddg` too, on any Python graph that has a self-loop; not fixed here — it is out of this leg's scope and belongs in a Python issue.)*

  The prefix is exact, verified rather than assumed: all 125,532 body nodes' ids start with their owning callable's id plus `@` (0 exceptions), no body node has two owners or none, and no callable id is another callable id plus `@`.
- [x] **Step 5: TypeScript's DDG has one provenance tier.** Every `TS_DDG` edge carries `prov == ["reaching-defs"]`; Python's `PROV_CERTAINTY` ranking of `points-to`/`reaching-defs`/`ssa` therefore collapses to a single value here. Keep the field and the ranking helper, state the single-tier fact in the docstrings and the lossiness table, and do not invent a tier.
  - *as done:* stated on `TSAnalysisBackend.get_ddg`, `TypeScriptAnalysis.get_ddg` and `TSNeo4jBackend.get_ddg`; `prov` and `prov_rank` are untouched. Two tests pin it, and the live one asks the **graph** for the whole `DISTINCT r.prov` set rather than checking one callable, so it fails the day the analyzer emits a second tier instead of quietly agreeing with a stale docstring.
- [x] **Step 6: Backend parity, then live scale.** Parity offline; then on 7692 run each accessor against a real callable and record wall-clock — `TSBodyNode` is 125,532 nodes and `TS_DDG` 119,384 edges, so a per-callable page must not scan the graph. A timing assertion must not run under coverage instrumentation (leg 1.5 measured ~5 s of instrumentation overhead on a large accessor); use the repo's `timed` marker convention.
  - *as done, and this is the first time the dual-backend claim is VERIFIED rather than reasoned.* The write-gated harness ran against a disposable Neo4j: **17 passed / 5 failed of 22**, and **all 11 Task-2 items passed** — the three per-callable graphs edge-for-edge over every callable, cursor-for-cursor paging, the slices over every entering value at three depths in both directions, the call-graph accessors over every callable, `reaches` over every ordered pair at two depths, both path queries hop-for-hop, both flow predicates in **both** halves of the asymmetry, and the miss paths. The five failures are all pre-existing 2.5a/Task-1 source-text accessors and are traced below.
  - *as done:* wall clocks on 7692 (median of 5, first discarded; each includes the `resolve_callable`/`resolve_value` round trip, ~25-30 ms of the total):

  | accessor | result | median |
  |---|---|---|
  | `get_ddg(drawGraph)` | 1,933 edges | 78.9 ms |
  | `get_cfg(drawGraph)` | 522 edges | 41.6 ms |
  | `get_cdg(drawGraph)` | 350 edges | 38.6 ms |
  | `get_ddg(DatabaseModal)` | 461 edges | 41.4 ms |
  | `callers_of(drawGraph)` | 1 | 39.1 ms |
  | `callees_of(drawGraph)` | 75 | 37.9 ms |
  | `reaches(...)` unbounded | `False` | 206.6 ms |
  | `backward_cone([...])` depth 5 / unbounded | 2 | 39.2 / 36.8 ms |
  | `slice_forward` depth 5 | 50 nodes | 53.0 ms |
  | `slice_forward` unbounded | 16,941 nodes | 366.2 ms |
  | `slice_backward` unbounded | 23,902 nodes | 389.2 ms |

  The timing assertion is `test_a_per_callable_page_does_not_scan_the_graph`, carrying `@pytest.mark.timed`; the `timed` marker's coverage-pausing hook was copied from `tests/analysis/python/conftest.py` into the TypeScript conftest, since a hookwrapper only applies to its own directory.
- [x] **Step 7: Commit** — `feat(typescript): per-callable graphs, slices, reachability and flow predicates`.
  - *as done:* offline TypeScript **372 passed / 110 skipped** (baseline 313/82); TypeScript live on 7692 **440 passed / 42 skipped** (baseline 363/32); release gate **1012 passed / 274 skipped** at **79.66%** (baseline 953/246 at 79.86%); Python live on 7689 **588 passed / 6 skipped**, the baseline exactly. Every delta accounted for in the report.

  **Three findings that are not this leg's code, recorded because they are load-bearing.**
  1. **`--emit neo4j` no longer accepts `-a`.** 1.3.0 exits 1 with "--analysis-level does not apply to --emit neo4j"; the harness passed `-a 2`, so *every* write-gated TypeScript live module has been unrunnable since Task 0's pin. Fixed in `_populate_neo4j` by dropping the flag.
  2. **1.3.0's live Bolt push silently drops every type node.** Pushing this app over `--neo4j-uri` leaves 0 `:TSClass` / `:TSInterface` / `:TSEnum` / `:TSTypeAlias` / `:TSNamespace` and no `TS_HAS_METHOD` / `TS_HAS_FIELD` / `TS_EXTENDS` / `TS_IMPLEMENTS`, so `TSNeo4jBackend` refuses the result at attach; the *same run's* `graph.cypher` carries all of them (6 classes, 2 interfaces, 2 enums, 1 alias, 1 namespace) and applies without a failed statement. `_populate_neo4j` now asks the emitter for its own projection and applies it verbatim.
  3. **1.3.0 projects `:TSCallable.code` one line short.** Measured against the same run's JSON: `src/index.main`'s graph `code` is 544 characters and the module's own span slice is 546 — `graph_code == local_code.rsplit("\n", 1)[0]` is exactly `True`, while `start_line`/`end_line` correctly say 6..23. That single defect is the root cause of four of the five parity failures (`get_method_bodies`, `get_source`, `describe`, and `locate_many`'s `source` comparison). The fifth, `test_callsites_parity`, is the decorator call site `@Param("id")`, whose graph body node carries only lines and the resolved callee (`method_name=''`, empty `argument_types`, `return_type=None`, columns `-1`) — the lossiness `TSNeo4jBackend`'s own module docstring already records, which that pre-existing test asserts past. **None of the five touches a Task-2 accessor**, and none is fixed here: softening someone else's parity assertion to green would hide an analyzer defect.

  One further harness correction: `ts_dual`'s local reference is now built at `system_dependency_graph`, not `call_graph`. It was matched to an emit run with `-a 2`; the emitter is now always full depth, so a level-2 reference compares two *analysis levels* rather than two backends — measured, `locate("src/controllers.ts", 4)` returned `body=None` locally (level 2 emits no body nodes) against the graph's `@entry`.

---

## Task 3: entrypoints, the artifact/config facade, the audit, docs

**Files:**
- Modify: both backends, `cldk/analysis/typescript/typescript_analysis.py`, `CHANGELOG.md`, `docs/agent-api-reference.md`, `CLAUDE.md`, the spec
- Test: new `tests/analysis/typescript/test_typescript_entrypoints.py`, `test_typescript_neo4j_multi_application_scope.py` (every new statement), `test_typescript_public_surface.py`

**Interfaces** — mirror exactly:
```
get_entrypoints(self) -> List[TSCallableOverview]
get_entrypoint_classes(self) -> List[TSClassOverview]
get_entrypoint_coverage(self) -> EntrypointCoverage
get_artifacts(self) -> Dict[str, PyArtifact]
get_dependencies(self, *, direct_only: bool = False, ecosystem: str | None = None, declared_in: str | None = None) -> List[PyDependency]
get_config_keys(self) -> Dict[str, PyConfigKey]
get_config_uses(self, key: str | None = None) -> List[PyConfigUseEdge]
get_unresolved_config_reads(self) -> List[PyConfigRead]
get_config_readers(self, key: str) -> List[TSCallableOverview]
```
The artifact five already exist on the **backends** (2.5a); this task puts them on the **facade**, which is where a caller reaches them.

- [x] **Step 1: Failing tests.** `get_entrypoints` returns overviews for the callables 1.3.0 marks; `get_entrypoint_coverage` reports the frameworks detected and the unresolved counts. **The graph carries the report as a JSON string property `entrypoint_report_json` on the anchor, plus `entrypoint_frameworks`** — measured on 7692, where it names `commander` and 66 unresolved selectors. Parse it; do not reconstruct it from nodes, and do not report `entrypoint_report_unavailable` when it is right there.
  - *as done:* 12 tests in `tests/analysis/typescript/test_typescript_entrypoints.py` (local backend over the a4 fixture, plus two fake-driver tests for the Neo4j report's two paths), 4 more on the live reference graph in `test_typescript_e2e_neo4j_live.py`, and 4 leak tests on the two-application fake graph. **Two of the step's numbers are wrong.** The report names **65** unresolved selectors (411 occurrences), not 66. And the graph marks **one** entrypoint callable and **zero** entrypoint classes — the report is the interesting half, not the list. The offline fixture is the complementary case and is what the failing tests were written against: **no** framework detected, **no** callable or type marked, and three near-misses (`Get` ×2, `Controller` ×1) — `get_entrypoints() == []` with a coverage record that says the silence is not clean, which is the whole reason the two accessors are separate.
  - *as done:* `is_entrypoint` is projected onto `:TSCallable` and `:TSClass` **only** — no other label carries the property at all on 7692, though the 1.3.0 schema declares it on all five type kinds. So `get_entrypoint_classes` is classes-only on both backends (widening it would make them disagree), and that is recorded on the ABC, on `TSClassOverview` and in the reference rather than left silent. There are no per-entrypoint nodes and no `entrypoints` property anywhere in the projection, which is the second reason the report is parsed rather than rebuilt.
- [x] **Step 2: Implement** on both backends; the facade delegates.
  - *as done:* ABC `cldk/analysis/typescript/backend.py:365-431`; local `codeanalyzer/codeanalyzer.py:750-806`; Neo4j `neo4j/neo4j_backend.py:1004-1087`; facade `typescript_analysis.py:740-882`. New model `TSClassOverview` in `cldk/models/typescript/projections.py:125`. The artifact five are **delegations only** — they were already on both backends from 2.5a and were not reimplemented. Two things the step did not say. (a) `get_config_readers` resolves a reading body node to its owner over `TS_HAS_BODY_NODE` (and over the callable's own `body` map in process), never by splitting the id on `@` the way the Python twin does — an anonymous callable's own id contains one, the same fact Task 1 hit in `get_source`. (b) `get_entrypoint_classes` carries the `kind = 'class'` guard alongside the label, the declaration-merge rule this file uses everywhere.
  - *as done:* the nine signatures are `PythonAnalysis`'s exactly, with only the projection types renamed; `tests/analysis/typescript/test_typescript_public_surface.py` now asserts that mechanically for **every** accessor both facades carry (54 of them), against a five-entry ledger of pre-existing 1.x divergences (`get_call_graph`, `get_classes`, `get_symbol_table` never gained Python's scoping keywords; `get_callers`/`get_callees` keep TypeScript's optional argument) that a second test forces to be deleted rather than left behind once closed.
- [x] **Step 3: The multi-application audit covers every statement this leg added** — class-level and inline, both prefixes, every variable not reachable from the anchor carrying the prefix predicate, and the driver-surface allow-list so a statement issued through a new driver API cannot escape the harvester (both hardenings that Java's leg-3a review forced; port them here rather than re-learning them).
  - *as done:* the harvester now sees **67** statements (63 before this task; the four new ones are `get_entrypoints`, `get_entrypoint_classes`, `get_entrypoint_coverage`, `get_config_readers`), and its expected-method list grew from 2.5a's twenty to include every leg-2.5b site. Both hardenings ported: `_DRIVER_SURFACE`/`_SESSION_SURFACE` allow-lists (`self._driver.{session,close}`, `self._session_obj.{run,close}`), and `_unscoped_variables`, which judges **per bound variable** instead of asking whether the text contains a predicate somewhere.
  - *as done, and the per-variable net found real leaks:* it flagged **14 of 67** statements on the first run, and each was fixed rather than excused. `_CALLERS`/`_CALLEES`/`_call_rows` scoped only one endpoint of a `TS_CALLS` edge — `_CALLERS` matched the *other* endpoint by a signature two applications can both declare, which is exactly the hole the hardening exists to catch; `_REACHES` left the quantified pattern's far end and its trailing node unscoped; `_CONE` its trailing node; `_SLICE` every body node it reached; `_heritage` the `TS_EXTENDS`/`TS_IMPLEMENTS` target. All seven now carry the two-prefix predicate on both ends. `get_config_readers` was rewritten to bind the reading callable as `c` directly, because a `WITH reader AS c` rename is a variable the audit cannot follow back to its scope — a statement should not need the auditor to be cleverer.
  - *as done:* three pieces of machinery the Java port needed on TypeScript's harder statements, each stated in the test's own docstrings. (a) `_render` resolves the `.format()` placeholders (`{{` → `{`, `{{0,{depth}}}` → `{0,5}`, `{left}`/`{right}`, `{rel}`/`{rels}`) so the audit reads the statement that runs — without it a template's `{{id:$src}}` is not an id lookup and its quantified pattern does not tokenise at all. (b) a paren-depth-aware clause splitter, because the naive one breaks a quantified path apart at the `WHERE` *inside* its parentheses and silently drops the pattern's own trailing node — the variable most likely to be the leak. (c) `_KEEPS_SCOPE` propagates **bidirectionally**, unlike Java's, because TypeScript reads containment backwards (`(o:TSClass)-[:TS_HAS_METHOD]->(c)` scopes `c` and reads `o` off it). `TS_CALLS` and the SDG edges are deliberately not in it, on Java's reasoning: both endpoints are application-owned and this SDK attaches to graphs it did not emit. Six self-tests pin the net against shapes it must reject and five it must accept.
  - *as done:* one harness bug found on the way — the fake Cypher evaluator could not read a **boolean literal** (`c.is_entrypoint = true`), so it silently ignored that conjunct and returned every callable. Fixed in `_COND`/`_value`; without it the four new leak tests would have passed for the wrong reason.
- [x] **Step 4: Docs.** `CHANGELOG.md`: the 31 new accessors grouped, the pin `1.2.0 → 1.3.0`, the new floor and what it refuses, TypeScript's single DDG provenance tier, and the lossiness rows this leg adds. `docs/agent-api-reference.md`: the TypeScript query section, replacing 2.5a's "the query surface arrives in 2.5b" line. `CLAUDE.md`: the TypeScript row's pin. The spec: TS-1 and TS-2 marked delivered, with the measured facts that differed.
  - *as done:* **the count is 29, not 31** — 28 methods on the frozen list (46 → 74) plus the `has_resolution_edges` property. CHANGELOG `[Unreleased]` gains `### Added` (the 29 accessors grouped in one entry, `TSClassOverview` in another) and four `### Changed` entries (the pin and floor with its migration line, `BodyRef`, the single DDG tier, the per-variable scoping), each one to three lines — python-sdk#350 is about a release whose notes ran to 5,429 words, so the detail is in the spec and this plan instead.
  - *as done:* `docs/agent-api-reference.md`'s TypeScript section replaces the "arrives in 2.5b" paragraph with what is there, plus the three TypeScript-specific facts a caller of the shared surface needs (one DDG provenance tier; a dotted `in_module=` strips neither `index` nor `__init__`; an `actual_in` has a position, not a name); the floor table moves to 1.3.0 and says what a 1.2.0 graph now does; five lossiness rows are added. Two pre-existing doc errors were corrected while there and are logged under `### Fixed`: `EntrypointCoverage.unresolved` is a `dict[str, int]`, not a `list[str]`, and `get_config_keys()` is keyed by the key's `can://` id, so the documented `get_config_keys()["DB_URL"]` never worked.
  - *as done:* **`get_config_keys`'s `can://` key was deliberately left alone** (python-sdk#346). It is a URI in a public return value *and* unusable across backends, since the id embeds the application name — but it is a cross-language break that Java has already made in its own leg, so changing it here would fix TypeScript into a third spelling. The reference now says so at the point of use rather than leaving a caller to find out.
- [x] **Step 5: The three runs, sequentially, verbatim** — the release gate, Python live on 7689, TypeScript live on 7692 (a new baseline; record it). Every delta explained test by test.
  - *as done:* release gate **1059 passed / 285 skipped**, coverage **80.14%** (baseline 1012/274 at 79.66%): +47 passed = 20 (multi-application audit) + 15 (frozen surface) + 12 (entrypoints), +11 skipped = 7 (write-gated parity) + 4 (live-only). TypeScript offline **419 passed / 121 skipped** (baseline 372/110). TypeScript live on 7692 — **the new baseline — 491 passed / 49 skipped** (baseline 440/42): the offline +47 plus 4 live entrypoint tests, and the same +7 write-gated skips. Python live on 7689 **588 passed / 6 skipped**, the baseline exactly — zero delta, which is the point of running it.
  - *as done:* the write-gated parity harness ran against the disposable graph on 7693: **24 passed / 5 failed of 29** (was 17/5 of 22). **All 7 Task-3 parity tests passed** — entrypoints, entrypoint classes, the coverage record (both backends supply it in full, so neither may answer with `diagnostics`), artifacts, dependencies and config keys, config uses and readers, and `get_unresolved_config_reads` as the one documented divergence (the projection carries no `config_reads`, so Neo4j raises where the local backend answers `[]`). The 5 failures are the same pre-existing source-text ones Task 2 traced to `:TSCallable.code` being projected one line short, plus the decorator call site; **none touches a Task-3 accessor**, and none was softened.
- [x] **Step 6: Commit** — `feat(typescript): entrypoints and the artifact layer on the facade; record the 1.3.0 query surface`.

## Definition of done

- Every accessor in Tasks 1–3 answers identically on `TSCodeanalyzer` and `TSNeo4jBackend` over the same application, with the same diagnostics on the miss paths.
- The frozen public surface grows by exactly this leg's accessors; no pre-existing signature changes.
- `GraphSchemaMismatch` refuses the 1.2.0 graph on 7690; the audit enumerates every statement over both prefixes.
- Bounds never silent; predicates unbounded by default; one completeness protocol.
- `cldk/analysis/commons/` imports no language package.
- One PR to `release/2.0`, `Closes #343`.

## Not in this plan

Java (#310, #311). The cross-language sweep. Analyzer work: cants#94/#95 (prefixing the bare `CanNode`/`Application` merge labels), #177 (declaration-merge id collision).
